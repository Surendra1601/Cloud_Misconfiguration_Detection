{
  "remediation_id": "REM_14",
  "title": "Secure Lambda Function Configuration",
  "domain": "serverless",
  "severity": "medium",
  "check_id": "CHECK_14",
  "console_steps": [
    "1. Open the Lambda console",
    "2. Select the function",
    "3. Under 'Configuration', click 'Environment variables'",
    "4. Remove any hardcoded secrets, use Secrets Manager references",
    "5. Under 'Permissions', verify execution role follows least-privilege",
    "6. Under 'VPC', configure VPC if function accesses private resources",
    "7. Set reserved concurrency to prevent abuse"
  ],
  "cli_command": "aws lambda update-function-configuration --function-name {FUNCTION_NAME} --environment 'Variables={DB_SECRET=arn:aws:secretsmanager:{REGION}:{ACCOUNT}:secret:{SECRET_NAME}}'",
  "cli_example": "aws lambda update-function-configuration --function-name my-function --environment 'Variables={DB_SECRET=arn:aws:secretsmanager:us-east-1:123456789012:secret:db-creds}'",
  "terraform_snippet": "resource \"aws_lambda_function\" \"example\" {\n  # ... other config ...\n\n  environment {\n    variables = {\n      DB_SECRET = aws_secretsmanager_secret.db.arn\n    }\n  }\n\n  reserved_concurrent_executions = 100\n\n  vpc_config {\n    subnet_ids         = var.private_subnet_ids\n    security_group_ids = [aws_security_group.lambda.id]\n  }\n}",
  "references": [
    {
      "framework": "CIS AWS Foundations Benchmark",
      "control_id": "4.1",
      "title": "Ensure Lambda functions do not have admin privileges"
    },
    {
      "framework": "NIST 800-53",
      "control_id": "AC-6",
      "title": "Least Privilege"
    },
    {
      "framework": "SOC 2",
      "control_id": "CC6.3",
      "title": "Role-based access"
    }
  ],
  "estimated_fix_time_minutes": 20,
  "risk_reduction": "Medium — reduces Lambda attack surface",
  "rollback_difficulty": "Medium — may affect function behavior"
}
