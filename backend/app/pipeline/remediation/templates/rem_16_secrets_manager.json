{
  "remediation_id": "REM_16",
  "title": "Migrate Secrets to AWS Secrets Manager",
  "domain": "data_protection",
  "severity": "high",
  "check_id": "CHECK_16",
  "console_steps": [
    "1. Open the Secrets Manager console",
    "2. Click 'Store a new secret'",
    "3. Choose secret type (RDS, Other database, or Other)",
    "4. Enter the secret key-value pairs",
    "5. Name the secret with a descriptive path",
    "6. Configure automatic rotation (recommended 30 days)",
    "7. Update application code to retrieve secret via SDK",
    "8. Remove hardcoded secrets from environment variables"
  ],
  "cli_command": "aws secretsmanager create-secret --name {SECRET_NAME} --secret-string '{SECRET_VALUE}'",
  "cli_example": "aws secretsmanager create-secret --name prod/myapp/db-password --secret-string '{\"username\":\"admin\",\"password\":\"s3cur3!\"}'",
  "terraform_snippet": "resource \"aws_secretsmanager_secret\" \"db\" {\n  name                    = \"prod/myapp/db-password\"\n  recovery_window_in_days = 7\n}\n\nresource \"aws_secretsmanager_secret_rotation\" \"db\" {\n  secret_id           = aws_secretsmanager_secret.db.id\n  rotation_lambda_arn = aws_lambda_function.rotate.arn\n\n  rotation_rules {\n    automatically_after_days = 30\n  }\n}",
  "references": [
    {
      "framework": "CIS AWS Foundations Benchmark",
      "control_id": "2.4",
      "title": "Ensure secrets are managed securely"
    },
    {
      "framework": "NIST 800-53",
      "control_id": "IA-5",
      "title": "Authenticator Management"
    },
    {
      "framework": "PCI DSS",
      "control_id": "8.2.1",
      "title": "Use strong cryptography to render passwords unreadable"
    }
  ],
  "estimated_fix_time_minutes": 30,
  "risk_reduction": "High — eliminates hardcoded secret exposure",
  "rollback_difficulty": "Medium — requires code changes to revert"
}
